# navigate to your repository if you're not already there
cd /path/to/your/repository
# stage the changes
git add .
# commit the changes
git commit -m "Your commit message"
# push the changes
git push origin master